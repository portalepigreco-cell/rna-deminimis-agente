# Dipendenze per scraping RNA
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Dipendenze per web app Flask
Flask>=2.3.0
Flask-CORS>=4.0.0

# Dipendenze per automazione browser (opzionale per produzione)
# NOTA: Versione fissata a 1.55.0 per allineamento con Docker image disponibile
playwright==1.55.0

# Dipendenze per OCR (opzionale)
pytesseract>=0.3.10
Pillow>=10.0.0

# Dipendenze per lettura PDF
pdfplumber>=0.10.0

# Dipendenze per analisi dati
pandas>=2.0.0
numpy>=1.24.0

# Server WSGI per produzione
gunicorn>=21.2.0
